# คู่มือการใช้ Git Flow ฉบับสมบูรณ์ (The Full Manual)

เอกสารนี้ไม่ใช่แค่กฎระเบียบ แต่คือ **"คัมภีร์" (Master Class)** สำหรับการบริหารจัดการ Code ของโปรเจกต์ Colestia เพื่อให้แน่ใจว่างานจะดำเนินไปอย่างรวดเร็วและระบบหลักไม่พัง

---

## 🏗️ ส่วนที่ 1: โครงสร้างและแนวคิด (The Architecture)

เราใช้ระบบที่เรียกว่า **Gitflow** ให้จินตนาการถึง "ร้านอาหาร":
*   **ห้องอาหาร (`master`)**: นี่คือสิ่งที่ลูกค้าเห็น ต้องสมบูรณ์แบบ สะอาด และพร้อมเสิร์ฟ ห้ามหั่นหัวหอมบนโต๊ะลูกค้าเด็ดขาด
*   **ห้องครัว (`develop`)**: นี่คือที่ที่ปรุงอาหาร อาจจะวุ่นวาย มีของวางระเกะระกะ และมีการเปลี่ยนแปลงตลอดเวลา
*   **เขียงส่วนตัว (`feature/*`)**: นี่คือที่ที่คุณยืนทำ *เมนูเฉพาะอย่าง* คุณจะไม่ทอดสเต็กและต้มซุปในกระทะเดียวกัน

### กฎเหล็ก (The Golden Rules)
1.  **ห้าม Push ขึ้น `master` โดยตรงเด็ดขาด** (นี่คือพื้นที่ศักดิ์สิทธิ์)
2.  **`master` คือความเสถียร** ถ้า Code อยู่บน master แปลว่ามันพร้อมใช้งานจริง
3.  **`develop` คือความปลอดภัย** มันอาจจะยังไม่เสร็จสมบูรณ์ แต่ต้องไม่ Error จนรันไม่ได้

---

## 🌿 ส่วนที่ 2: กิ่งก้านสาขา (The Branches)

### 1. `master` (Production / ของจริง)
*   **หน้าที่**: เวอร์ชันที่ใช้งานจริงบนเว็บ
*   **การอัปเดต**: เฉพาะตอน "Release" เวอร์ชันใหม่เท่านั้น (เช่น v1.0 -> v1.1)

### 2. `develop` (Integration / ห้องครัวรวม)
*   **หน้าที่**: พื้นที่รวมงานของทุกคน
*   **การอัปเดต**: เมื่อ Feature ไหนเสร็จและตรวจแล้ว จะถูกนำมารวมที่นี่

### 3. `feature/name-of-feature` (Work / เขียงส่วนตัว)
*   **หน้าที่**: พื้นที่ทดลองและทำงานสำหรับฟีเจอร์ใดฟีเจอร์หนึ่งโดยเฉพาะ
*   **สร้างจาก**: `develop`
*   **รวมกลับไปที่**: `develop`
*   **ตัวอย่าง**: `feature/cart-system` (ระบบตะกร้า), `feature/login-page` (หน้าล็อกอิน)

### 4. `fix/name-of-bug` (Repair / ซ่อมแซม)
*   **หน้าที่**: แก้บั๊กที่เจอระหว่างพัฒนา
*   **สร้างจาก**: `develop`
*   **รวมกลับไปที่**: `develop`
*   **ตัวอย่าง**: `fix/header-typo` (แก้คำผิดหัวเว็บ)

---

## 🎓 ส่วนที่ 3: บทเรียนการทำงาน (Step-by-Step Tutorials)

### บทเรียน A: การสร้างฟีเจอร์ใหม่ (New Feature)
*โจทย์: คุณต้องการสร้างหน้า "About Us" (เกี่ยวกับเรา)*

**ขั้นตอนที่ 1: เตรียมห้องครัว (Update Develop)**
ก่อนเริ่มงาน ต้องดึง Code ล่าสุดของเพื่อนๆ มาก่อนเสมอ
```bash
git checkout develop
git pull origin develop
```

**ขั้นตอนที่ 2: เข้าประจำที่ (Create Branch)**
สร้าง Branch ส่วนตัวแยกออกมาทำงาน
```bash
git checkout -b feature/about-page
```

**ขั้นตอนที่ 3: ปรุงอาหาร (Coding)**
*   สร้างไฟล์ `About.jsx`
*   ใส่ CSS ตกแต่ง
*   *ทริค: พยายาม Commit งานบ่อยๆ*

**ขั้นตอนที่ 4: ห่อกลับบ้าน (Commit)**
บันทึกงานที่คุณทำเสร็จแล้วลงกล่อง
```bash
git add .
git commit -m "feat(pages): creating initial layout for about page"
```

**ขั้นตอนที่ 5: ส่งงาน (Push)**
ส่ง Branch ของคุณขึ้น GitHub เพื่อให้คนอื่นเห็น
```bash
git push -u origin feature/about-page
```

**ขั้นตอนที่ 6: ขอการตรวจ (Pull Request)**
*   ไปที่เว็บ GitHub
*   กดปุ่ม "Compare & pull request" จาก `feature/about-page` ไปยัง `develop`
*   รอเพื่อนมาตรวจ แล้วกด "Merge"

---

### บทเรียน B: การแก้บั๊ก (Fixing Bugs)
*โจทย์: ปุ่มกดสีผิด หรือพิมพ์ผิด*

**ขั้นตอนที่ 1: ดึง Code ล่าสุด**
```bash
git checkout develop
git pull origin develop
```

**ขั้นตอนที่ 2: สร้าง Branch แก้ไข**
```bash
git checkout -b fix/submit-button-color
```

**ขั้นตอนที่ 3: แก้และ Commit**
```bash
# ... แก้ CSS ให้ถูกต้อง ...
git add .
git commit -m "fix(ui): correct submit button hex code"
```

**ขั้นตอนที่ 4: ส่งงานและรวม**
```bash
git push -u origin fix/submit-button-color
# ไปกด Pull Request ที่ GitHub เพื่อรวมเข้า develop
```

---

### บทเรียน C: การปล่อยเวอร์ชันใหม่ (The Release)
*โจทย์: งานใน `develop` พร้อมแล้ว จะปล่อยเวอร์ชัน 1.1.0 ให้โลกเห็น*

**ขั้นตอนที่ 1: ปรับเลขเวอร์ชัน (ใน Code)**
ยังอยู่ที่ `develop` ให้เปิดไฟล์ `package.json` แล้วแก้เลขเวอร์ชัน
```json
// package.json
{
  "name": "colestia",
  "version": "1.1.0",  // เปลี่ยนจาก 1.0.0
  ...
}
```
บันทึกการเปลี่ยนเลขเวอร์ชัน:
```bash
git commit -am "chore(release): bump version to 1.1.0"
git push origin develop
```

**ขั้นตอนที่ 2: สลับไป Production**
```bash
git checkout master
git pull origin master
```

**ขั้นตอนที่ 3: รวมร่าง (Merge)**
ดึงของใหม่จาก `develop` เข้า `master`
```bash
git merge develop
```

**ขั้นตอนที่ 4: แปะป้าย (Tag)**
ติดป้ายบอกว่าจุดนี้คือ v1.1.0 อย่างเป็นทางการ
```bash
git tag -a v1.1.0 -m "Release Version 1.1.0"
```

**ขั้นตอนที่ 5: ขึ้นระบบ (Go Live)**
ส่งทั้ง Code และ Tag ขึ้น Server
```bash
git push origin master
git push origin v1.1.0
```

---

### บทเรียน D: สถานการณ์ "Merge Conflict" (รถชนกัน!) �
*โจทย์: นาย A แก้บรรทัดที่ 10 ของไฟล์ Header.jsx และนาย B ก็แก้บรรทัดที่ 10 ของไฟล์เดิมเหมือนกัน Git ไม่รู้จะเชื่อใคร!*

**อาการ**:
เมื่อคุณสั่ง `git pull` หรือ `git merge` แล้วเจอข้อความ:
> *CONFLICT (content): Merge conflict in src/components/Header.jsx*
> *Automatic merge failed; fix conflicts and then commit the result.*

**วิธีแก้ไข (Step-by-Step):**

**1. ตั้งสติและเปิดไฟล์ที่พัง**
เปิดไฟล์ที่ Git แจ้ง (เช่น `Header.jsx`) คุณจะเห็นเครื่องหมายประหลาดๆ แบบนี้:

```jsx
<<<<<<< HEAD
<h1>ยินดีต้อนรับสู่ Colestia (แก้อันนี้)</h1>
=======
<h1>Welcome to Colestia (หรืออันนี้ดีกว่า)</h1>
>>>>>>> feature/new-header
```

*   `<<<<<<< HEAD`: คือ Code ปัจจุบันที่คุณมีอยู่ (My Code)
*   `=======`: เส้นแบ่งเขต
*   `>>>>>>> feature/...`: คือ Code ที่เข้ามาใหม่ (Incoming Code)

**2. เลือกผู้ชนะ (Manual Fix)**
คุณต้องลบเครื่องหมายพวกนี้ทิ้ง แล้วเลือก Code ที่ถูกต้อง หรือจะผสมมันก็ได้ เช่น:

*เปลี่ยนเป็น:*
```jsx
<h1>Welcome to Colestia Thailand</h1>
```
*(ลบ `<<<`, `===`, `>>>` ทิ้งให้หมด เหลือแต่ Code ที่ใช้ได้จริง)*

**3. บอก Git ว่าแก้แล้ว**
เมื่อแก้ครบทุกจุด ให้สั่ง:
```bash
git add .
```

**4. จบงาน (Continue)**
```bash
git commit -m "fix: resolve merge conflict in header"
# ถ้าติดตอน pull ให้สั่ง git push ต่อได้เลย
```

---

## 📝 ส่วนที่ 4: การเขียน Commit ให้เป็นมืออาชีพ

เราใช้ระบบ **Conventional Commits** (เขียนยังไงให้อ่านรู้เรื่อง)

**รูปแบบ**: `ประเภท(ส่วนที่แก้): อธิบายสั้นๆ`

| ประเภท (Type) | ความหมาย | ตัวอย่าง |
| :--- | :--- | :--- |
| **feat** | ฟีเจอร์ใหม่ | `feat(auth): เพิ่มระบบล็อกอินด้วย Google` |
| **fix** | แก้บั๊ก | `fix(nav): แก้ลิ้งค์ตายใน footer` |
| **docs** | แก้เอกสาร | `docs(readme): เพิ่มวิธีติดตั้ง` |
| **style** | จัดหน้า/เว้นวรรค | `style(home): จัดย่อหน้า code ใหม่` |
| **refactor** | รื้อโค้ด (ผลลัพธ์เหมือนเดิม) | `refactor(api): เขียน logic การดึง user ใหม่` |
| **chore** | งานจิปาถะ | `chore(deps): อัปเดต version ของ react` |

---

## ❓ ส่วนที่ 5: คำถามที่พบบ่อย (FAQ)

**Q: เผลอ Commit ลง `develop` หรือ `master` ไปแล้วทำไง?**
A: อย่าเพิ่งตกใจ
1.  รีบสร้าง branch ใหม่เพื่อเซฟงานตัวเองไว้ก่อน: `git checkout -b feature/my-saved-work`
2.  งานของคุณจะปลอดภัยอยู่ที่ branch ใหม่แล้ว
3.  กลับไปที่ develop แล้วรีเซ็ตมัน (ระวัง!): `git checkout develop`, `git reset --hard origin/develop`


---

### บทเรียน E: เมื่อเพื่อนไปไกลแล้ว (Syncing / Updating) 🐢
*โจทย์: คุณแยกออกมาทำ Feature ตั้งแต่เมื่อวาน แต่วันนี้เพื่อนในทีม Merge งานใหม่ๆ เข้า `develop` ไปเยอะแล้ว Code ของคุณเริ่ม "เก่า" (Outdated)*

**ความเสี่ยง**: ถ้าคุณฝืนทำต่อแล้ว Merge กลับไป อาจจะเกิด Conflict มหาศาล หรือทำให้ฟีเจอร์ของเพื่อนพัง

**วิธีแก้ไข: ดึงของใหม่มาผสม (Sync with Develop)**

1.  **อัปเดต Develop ของเราให้ใหม่ล่าสุดก่อน**
    ```bash
    git checkout develop
    git pull origin develop
    ```

2.  **กลับมาที่งานของเรา**
    ```bash
    git checkout feature/about-page
    ```

3.  **ดูดพลัง (Merge) จาก Develop เข้ามา**
    คำสั่งนี้จะบอกว่า *"เอาของใหม่ๆ ที่เพื่อนทำเสร็จแล้ว มาใส่ในงานของฉันด้วย"*
    ```bash
    git merge develop
    ```
    *(ถ้ามี Conflict ให้แก้ตามบทเรียน D)*

4.  **ทำงานต่อได้เลย!**
    ตอนนี้งานของคุณทันสมัยแล้ว (Up-to-date)

---

### ⚠️ คำเตือนสำคัญสำหรับการ Release (Master)
**คำถาม**: *"ถ้า Develop ในเครื่องเราเก่า (Old) เราจะ Merge เข้า Master ได้ไหม?"*
**คำตอบ**: **ห้ามเด็ดขาด!**

ก่อนจะ Release ขึ้น Master **ทุกครั้ง** ให้ทำ 2 สเต็ปนี้ก่น:
1.  `git checkout develop` และ `git pull origin develop` (เพื่อให้มั่นใจว่าเรามีทุกอย่างที่ทีมทำ)
2.  จากนั้นค่อย merge เข้า `master`

*กฎ: อย่าปล่อยของเก่าขึ้น Production*
